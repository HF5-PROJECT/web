---
import Layout from "../layouts/Layout.astro";
import HotelCard from "../components/hotel/card.vue";

const hotels = await (
  await fetch(import.meta.env.PRIVATE_API_URL + "/hotel", {
    method: "GET",
  })
).json();
---

<Layout title="Welcome to Astro.">
  <main class="flex flex-wrap justify-center gap-8 p-24">
    {hotels.map((hotel: any) => <HotelCard client:load hotel={hotel} />)}
  </main>
</Layout>

<style></style>
